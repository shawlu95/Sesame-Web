{
  "name": "sesame-web",
  "version": "1.0.0",
  "description": "Sesame Web",
  "main": "index.js",
  "scripts": {
    "test": "set NODE_ENV=test && mocha ./server/test",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "dev": "NODE_ENV=dev nodemon server/index.js",
    "start": "NODE_ENV=prod node server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^1.20.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.2",
    "express-rate-limit": "^6.0.4",
    "morgan": "^1.10.0",
    "promise": "^8.0.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^2.0.1",
    "mocha": "^2.5.3",
    "nodemon": "^2.0.15"
  }
}